#!/usr/bin/env bash
# This script is displaying "Best School" 10 times

i=0
n=10
nine=8
while (( i < n )); do
    array[i]='Best School'
    echo "${array[$i]}"
    if [[ "$i" -eq "$nine" ]]; then
        echo "Hi"
    fi
    ((i++))
done